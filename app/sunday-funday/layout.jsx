export const metadata = {
  title: 'Sunday Funday | Velo Performance Lab',
  description: 'Select Sunday small-group baseball and softball camps in Apollo Beach.',
};

export default function SundayFundayLayout({ children }) {
  return children;
}
